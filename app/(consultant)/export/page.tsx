// Export Manager — Server Component
// CSV export to CYF / Aclymate / Persefoni.

export default async function ExportPage() {
  return (
    <div>
      <h1>Export Manager</h1>
      {/* TODO: export format selector and download */}
    </div>
  );
}
